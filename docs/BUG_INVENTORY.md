# BUG_INVENTORY

higgsfield-auto-reel の独立レビュー(codex)＋目視検収で確定した不具合の棚卸し。
2026-07-17 に全件修正・検証済み（BUG-5は窓口目視・2周目で追加発見／BUG-6〜9は窓口統合検証で発見／BUG-10は窓口実機・実生成失敗で発見）。

| ID | 重大度 | 発見経路 | 症状 | 根本原因 | 修正内容 | 検証方法 | 状態 |
|---|---|---|---|---|---|---|---|
| BUG-1 | 高 | codex + 目視 | 生成リールの全ショットで、同一のキャプション文言が画面上に上下2箇所（mockクリップ焼き込み分＋ASS字幕分）表示される二重表示。 | `pipeline/visual/mock_backend.py` の `build_mock_cmd()` が `drawtext` で `shot["caption_jp"]` をクリップに直接焼き込んでいた。一方 `render.py`（`build_final_cmd`）は同じキャプションをASS字幕として別途焼く。両方が有効なため二重表示になる。 | `mock_backend.py` からキャプションdrawtextを削除。代わりにショット識別用の小さく半透明（`fontcolor=white@0.5`, `fontsize=28`）なショット番号（例:`#1`）を右上に描画する `_shot_number_label()` を追加。キャプション表示はASS字幕（`subtitles.py`/`render.py`）に一本化。 | ユニットテスト: `tests/test_mock_backend.py::test_build_mock_cmd_does_not_burn_caption_text_bug1`（`-vf` 文字列にcaption_jp本文が含まれないこと、`drawtext`自体は番号表示のため残ることを確認）／`test_build_mock_cmd_no_drawtext_when_shot_id_missing`。E2E実測: `--theme "毎日5分でできる片付け習慣" --duration 15 --backend mock --no-llm` の出力mp4から2フレーム(`ffmpeg -ss 2`, `-ss 8`)を抽出し目視確認 — キャプションは画面下部に1箇所のみ、右上に小さく `#1`/`#3` の番号のみ表示。二重表示は解消。 | 修正済み・検証済み |
| BUG-2 | 中 | codex + 目視 | ASS字幕の自動改行が文字数ベース（13字固定カット）のため、「リセット」のような単語の途中（例:「リセ」／「ット」）で折り返されることがある。 | `pipeline/subtitles.py` の `wrap_caption_kinsoku()` が禁則文字（行頭に来てはいけない文字）だけを考慮し、単語・文節境界を一切考慮せず `max_chars` で機械的にカットしていた。 | `_find_bunsetsu_break()` を追加。句読点（、。！？）および助詞語尾（は/が/を/に/で/と/も/の/へ/や、から/まで/より/ので/のに 等、長い語尾から優先判定）で終わる位置のうち `max_chars` 以内で最も右側のものを改行位置として優先する簡易文節分割を実装。該当箇所が無ければ従来の文字数ベース禁則改行にフォールバックする（既存テストの挙動を保持）。 | ユニットテスト: `tests/test_subtitles.py::test_wrap_caption_kinsoku_bug2_does_not_split_word_mid_way`（「毎日5分でできる…」ではなく「毎日5分で散らかる前にリセット」が `["毎日5分で散らかる前に", "リセット"]` に分割され「リセット」が1行にまとまることを確認）／`test_find_bunsetsu_break_prefers_particle_boundary`。既存の禁則改行テスト（`test_wrap_caption_kinsoku_long_text_wraps_to_two_lines`, `test_wrap_caption_kinsoku_avoids_forbidden_line_start`）は無改修で通過（フォールバック経路が従来どおり機能することの回帰確認）。 | 修正済み・検証済み |
| BUG-3 | 高（P1） | codex | QAチェック（解像度/尺/音声/黒フレーム/ファイルサイズ）が不合格（`overall_ok=false`）でも、パイプライン全体は成功扱い（`report["ok"]=True`、`main()`の exit code 0）になっていた。 | `run.py` Stage 8（QA）で `report["stages"]["qa"]["overall_ok"]` は記録するが、stage自体の `ok` フィールドを更新していなかった。`_timed_stage` の `__exit__` は例外が無ければ `entry.setdefault("ok", True)` で無条件に True を補完するため、QA不合格でも当該stageは `ok=True` のまま。line 292付近の `report["ok"] = all(v.get("ok", False) for v in report["stages"].values())` がこの偽の `ok=True` を拾って全体成功と判定していた。 | `run.py` のQA stage内で `report["stages"]["qa"]["ok"] = qa_report["overall_ok"]` を明示的に設定するよう1行追加。これにより `_timed_stage.__exit__` の `setdefault` は上書きせず、QA不合格が `report["ok"]=False` に正しく伝播し、`main()` の `return 0 if report.get("ok") else 1` により exit code が非0になる。 | ユニットテスト: `tests/test_run.py::test_run_pipeline_qa_failure_makes_report_not_ok`（`qa_check.run_qa` を monkeypatch で `overall_ok=False` を返すよう差し替え、`run_pipeline()` を実行して `report["stages"]["qa"]["ok"] is False` かつ `report["ok"] is False` を確認。exit code相当の `0 if report.get("ok") else 1` も1であることを確認）。実測: `.venv/bin/python -m pytest -q tests/test_run.py -k qa_failure -v` → `1 passed`。尺を実データで極端に外す方法ではなく、QA判定関数自体を差し替える手段で実証（正直な報告として明記）。 | 修正済み・検証済み |
| BUG-4 | 中（P2） | codex | `config.json` に `brand_rules.ng_words` が未設定/空の場合、`compliance.DEFAULT_NG_WORDS`（「絶対稼げる」「100%成功」「みお」等の景表法NG表現・競合名義）による検査が事実上無効化されていた。 | `run.py:148-149`（旧コード）で `ng_words = (cfg.get("brand_rules") or {}).get("ng_words", [])` としていたため、config未設定時は `[]` が `compliance.check_plan(plan, ng_words=[])` に渡り、`check_plan()` 内の `ng_words = ng_words if ng_words is not None else DEFAULT_NG_WORDS` が「空リストはNoneではない」ため デフォルトへフォールバックせず、NGワード検査が実質ゼロ件になっていた。さらに `pipeline/config.py` の `_DEFAULTS` も `"brand_rules": {"ng_words": []}` であるため、config.json自体が存在しない場合も同じ穴が生じていた。 | `run.py` に `resolve_ng_words(cfg)` ヘルパーを新設。`compliance.DEFAULT_NG_WORDS` を常にベースとし、config側の `ng_words`（無ければ空扱い）を重複除去しつつ追加分としてマージする方式に変更（マージ方式を採用。デフォルトは常に有効、config側は追加ワード用）。 | ユニットテスト: `tests/test_run.py::test_resolve_ng_words_uses_defaults_when_brand_rules_missing` / `test_resolve_ng_words_uses_defaults_when_ng_words_key_missing` / `test_resolve_ng_words_uses_defaults_when_ng_words_is_empty_list` / `test_resolve_ng_words_merges_config_additions_with_defaults` / `test_compliance_blocks_ng_theme_even_without_config_ng_words`。実測: `config.json` を一時退避し `brand_rules` を `{}`（ng_words無し）に書き換えた状態で `.venv/bin/python run.py --theme "絶対稼げる副業術" --duration 15 --backend mock --no-llm` を実行 → `compliance: FAIL` で `word: '絶対稼げる'` の違反が5箇所（concept/hook/narration_script/shots[s1].caption_jp/shots[s1].visual_prompt）検出され exit code 1。実行後 `config.json` を元の内容に復元し `git status --short config.json` で差分なしを確認。 | 修正済み・検証済み |
| BUG-5 | 中 | 窓口目視（2周目・BUG-2修正の取り残し） | ASS字幕の簡易文節分割（BUG-2修正）が「毎日5分でできる片付け習慣を始めよう」を「毎日5分でできる片付け習慣を始めよう」のように「できる」の語中（「でで」／「きる」）で改行することがある。 | `pipeline/subtitles.py` の `_find_bunsetsu_break()` が助詞語尾「で」で終わる位置を無条件に改行境界として採用しており、「でできる」のように助詞「で」の直後に動詞/補助動詞の一部としてひらがなが続くケース（「できる」の先頭の「で」）と、真の格助詞「で」を区別できていなかった。加えて `wrap_caption_kinsoku()` はテキスト全体が `max_chars` 以内に収まる場合でも文節探索を先に行うため、短文中に助詞が含まれると不要な改行が発生する余地があった。 | (1) `_find_bunsetsu_break()` に「助詞境界（句読点以外）は直後の文字がひらがな以外（漢字・カタカナ・英数字等）の場合にのみ有効」という判定を追加（`_is_hiragana()` 新設、`_PUNCTUATION_BREAK_ENDINGS` で句読点は従来通り無条件有効のまま除外）。(2) `wrap_caption_kinsoku()` の冒頭に `len(text) <= max_chars` なら即 `[text]` を返すガードを追加し、短文が助詞を含んでいても改行されないようにした。 | ユニットテスト4件追加（`tests/test_subtitles.py`）: `test_wrap_caption_kinsoku_bug5_particle_de_not_confused_with_verb_dekiru`（「できる」が割れない）／`test_wrap_caption_kinsoku_bug5_does_not_regress_bug2`（BUG-2回帰なし、`lines[1]=="リセット"`）／`test_wrap_caption_kinsoku_bug5_breaks_at_natural_position`（「範囲」「決める」が割れない自然な位置で改行）／`test_wrap_caption_kinsoku_bug5_short_text_no_wrap_even_with_particle`（助詞含み短文は改行なし）。既存67件は無改修のまま全通過（`git stash`で修正前67 passed→修正後71 passed、うち新規4件を実測比較）。E2E実測: `.venv/bin/python run.py --theme "毎日5分でできる片付け習慣" --duration 15 --backend mock --no-llm` を実行し `work/<run_id>/subtitles.ass` を直接catして `Dialogue: 0,0:00:00.00,0:00:03.00,Big,,0,0,0,,毎日5分でできる片付け習慣\Nを始めよう` を確認、さらに `./bin/ffmpeg -ss 1.0 -frames:v 1` で出力mp4からフレームを1枚抽出（`work/inspect/bug5_frame.png`）し目視で「毎日5分でできる片付け習慣」／「を始めよう」の2行表示・「できる」非分割を確認。 | 修正済み・検証済み |
| BUG-6 | 高（統合） | 窓口統合検証 | `GET http://127.0.0.1:8787/` が404。studio/web/ のSPAがサーバ単体では配信されず、別のローカルWebサーバを併用しないと画面を開けなかった（同一オリジンでのAPI疎通が未検証だった）。 | `studio/server/app.py` に studio/web/ を配信するマウントが存在しなかった。`/media/*` 系マウントのみ登録されており、`/`・`/app.js`・`/styles/*.css` 等アプリ本体のパスに対応するルート/マウントが無かった。 | `app.py` の末尾（既存の `/api/*` ルート・`/media/*` マウントの登録より後）に `app.mount("/", StaticFiles(directory=str(_STUDIO_WEB_DIR), html=True), name="web_spa")` を追加。Starletteはルート登録順に最初にマッチしたものを採用するため、この末尾マウントは他の全ルートに一致しなかった残りのパスのみを studio/web/ から静的配信する「最後の受け皿」として機能し、`/api/*` ・ `/media/*` とは衝突しない。 | 統合スモーク実測（サーバ再起動後）: `curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8787/` → `200`／`/app.js` → `200`（`content-type: text/javascript; charset=utf-8`）／`/styles/tokens.css` → `200`／`/components/timeline.js` → `200`／`/api/projects` → `200`（body `[]`）。ヘッドレスChrome（`Google Chrome --headless=new`）で `http://127.0.0.1:8787/`（mockなし・実API）を開きスクリーンショット取得→目視で「プロジェクトがありません」の空状態が実APIレスポンス（0件）から描画されていることを確認（`/tmp/higgsfield_shots/real_api_home.png`）。 | 修正済み・検証済み |
| BUG-7 | 低（UX） | 窓口統合検証（目視） | ヘッダーの「保存」「書き出し」ボタンは、プロジェクト未選択時にHTML属性としては `disabled` が付与されクリック不可だったが、見た目上は通常のボタンと区別がつかず「押せそう」に見えていた（`.btn` に `:disabled` 用のCSSが未定義）。 | `studio/web/styles/components.css` の `.btn` に `:hover:not(:disabled)` `:active:not(:disabled)` はあったが、`:disabled` 自体の外観（不透明度・カーソル）を定義するルールが無かったため、`--accent2` 等の塗り色がそのまま表示され押せる見た目のままだった。 | (1) `components.css` に `.btn:disabled, .btn[aria-disabled="true"] { opacity: 0.4; cursor: not-allowed; pointer-events: none; }` を追加。(2) `app.js` の `renderHeader()` で保存/書き出しボタンに `aria-disabled` 属性を明示的に付与（`saveDisabled`/`renderDisabled` 変数で条件を一本化）。 | ヘッドレスChromeスクリーンショット実測（`?mock=1`・プロジェクト未選択画面）: `/tmp/higgsfield_shots/mock_noproject.png` を目視確認。ヘッダーの「保存」「書き出し」ボタンがいずれも薄い色（opacity 0.4）で表示され、プロジェクト選択時（`/tmp/higgsfield_shots/mock_home.png`、書き出しボタンは鮮やかな黄色）とはっきり区別できることを確認（切り出し画像 `/tmp/higgsfield_shots/zoom_buttons.png`）。 | 修正済み・検証済み |
| BUG-8 | 低（UX） | 窓口統合検証（目視） | タイムラインでSFXマーカー（◆型）がBGM帯の上に直接重ねて描画されており、BGM帯とSFXマーカーが視覚的に衝突していた。 | `studio/web/components/timeline.js` で `sfxHtml` を `bgmHtml` と同じ `<div class="timeline-track timeline-track--bgm">` 内に描画していたため、両者が同一トラック行を共有していた。 | タイムラインに `timeline-track--sfx` の専用行を追加（時間軸=`PX_PER_SEC`は共有のまま）。`timeline.js` のトラックラベル（時間/BGM/SFX/ショット）とトラック本体を分離し、SFXマーカーはこの専用行にのみ描画。クリック時のインスペクタSFX行ハイライト（`highlightSfx`）は既存動作を維持。`components.css` に `.timeline-track--sfx { height: 28px; }` を追加し、`.bgm-band`/`.sfx-marker` の上下オフセットを新しい行の高さに合わせて調整。 | ヘッドレスChromeスクリーンショット実測（`?mock=1&open=p_ready001`、BGM=upbeat_01.mp3・SFX2件のプロジェクト）: `/tmp/higgsfield_shots/mock_home.png` を目視確認。タイムラインに「BGM」行と「SFX」行が独立して並び、SFXの◆マーカー2個がBGM帯の下の専用行に表示され重なっていないことを確認。 | 修正済み・検証済み |
| BUG-9 | 低（UI） | 窓口統合検証（目視） | プロジェクト未選択時のタイムライン空状態プレースホルダ文言「プロジェクトを開くとタイムラインが表示されます」が、`max-width: 240px` の制約下で「表示さ／れます」のように語の途中で不自然に改行されていた。 | `studio/web/styles/components.css` の `.state-block__desc { max-width: 240px; }` に改行制御指定が無く、長い文言が狭い幅に収まらず任意の文字位置で折り返されていた。 | (1) `timeline.js` の当該文言を「プロジェクトを開くとタイムラインが表示されます」→「プロジェクトを開くと表示されます」に短縮（同じ意味を保ちつつ1行に収まる長さへ）。(2) `components.css` の `.state-block__desc` を `max-width: 280px` に広げ、`word-break: keep-all; overflow-wrap: normal;` を追加して不要な分割を抑制。 | ヘッドレスChromeスクリーンショット実測（`?mock=1`・プロジェクト未選択画面）: `/tmp/higgsfield_shots/mock_noproject.png` から該当領域を切り出し（`/tmp/higgsfield_shots/zoom_timeline_empty.png`）、「プロジェクトを開くと表示されます」が1行で語中分割なく表示されることを目視確認。 | 修正済み・検証済み |
| BUG-10 | 高 | 窓口実機・実生成失敗 | `POST /api/projects {backend:"higgsfield"}` によるフルリール生成が失敗する。加えて、失敗時に `project.json` へ `error` は記録されるが `plan.shots` が0件のまま（=directorが立てた企画がまるごと失われる）だった。 | (1) directorが3秒ショットを計画する一方、`pipeline/visual/higgsfield_backend.py` の `_build_create_cmd`/`_build_cost_cmd` がショットの `duration_sec`（3秒）をそのまま `higgsfield generate create/cost --duration` に渡していた。実機で `seedance_2_0_mini` モデルの最小durationが4秒であることが判明し、CLIが `duration: Input should be greater than or equal to 4` というバリデーションエラー(exit code 3)を返しジョブが1件も投入できなかった。 (2) `studio/server/jobs.py` の `JobManager._run_generate` が、ビジュアル生成ループを完走した後に初めて `project["plan"]` へ企画内容を書き込んでいたため、ループ途中（1本目から）で失敗すると `project["plan"]` は `create_project()` 時点の空プレースホルダ（`shots: []`）のままになり、`error` フィールドだけが更新されて企画そのものが失われていた。 | (1) `higgsfield_backend.py` に `_resolve_request_duration_sec()` を追加し、CLIへ実際にリクエストする秒数を `clamp(ceil(shot["duration_sec"]), 4, 12)` に統一（`_MIN_REQUEST_DURATION_SEC=4` は実機エラーで確認した値、`_MAX_REQUEST_DURATION_SEC=12` は暴走防止の保守的な上限としてコメントで出典を明記）。`_build_create_cmd`/`_build_cost_cmd` 両方をこのヘルパー経由に統一し、見積コストと実際にリクエストする尺を一致させた。最終カットの尺は従来通り `render.build_normalize_clip_cmd(duration_sec=shot["duration_sec"])` が計画どおりにトリムする（この後段トリムは既存実装のままで、requestとcut尺を分離する設計）。 (2) `jobs.py` の `_run_generate` で、director企画が確定しコンプライアンス検査を通過した直後に、`clip_path=None` の状態で `plan.shots`（prompt/caption/尺/trimを含む）を即座に保存するよう変更。以後、各ショットの生成が1本成功するたびにその場で `clip_path` を確定して再保存し、途中で後続ショットが失敗しても、それまでの成功分＋未着手分の企画情報が失われないようにした。 | ユニットテスト: `tests/test_higgsfield_backend.py` に7件追加（`test_resolve_request_duration_sec_clamps_below_minimum_to_4_bug10`＝3秒→4秒、`test_resolve_request_duration_sec_clamps_above_maximum_to_12_bug10`＝18秒→12秒、`test_resolve_request_duration_sec_ceils_fractional_seconds_bug10`＝4.1秒→5秒（roundなら4になるところをceilで5にする回帰確認）、`test_resolve_request_duration_sec_keeps_value_within_range_unchanged_bug10`、`_build_create_cmd`/`_build_cost_cmd` のクランプ反映確認3件）。新規 `tests/test_jobs_generate.py` に2件追加（`test_run_generate_preserves_plan_shots_when_first_shot_fails_bug10`＝バックエンドが1本目から常に失敗するケースで `plan.shots` が3件保持され `clip_path` は全てNoneのまま企画内容(prompt/caption/trim)が残ることを確認、`test_run_generate_preserves_earlier_successful_clip_path_when_later_shot_fails_bug10`＝1本目成功・2本目失敗のケースで1本目のみ`clip_path`が確定し2本目以降はNoneのまま保持されることを確認）。実測: `.venv/bin/python -m pytest -q` で修正前143件→修正後152件、全件パスを確認（`git stash`で修正前後を実測比較）。higgsfield実生成（課金）そのものでの最終検証は窓口が別途実施する。 | 修正済み・検証済み（ユニットテストレベル。実CLIでの最終検証は窓口実施） |
