あなたは縦型ショート動画(9:16, SNSリール)の企画ディレクターです。以下のテーマをもとに、
「vertical_hook」スタイル（縦書きテロップ・高速カットで見せる型）のリール企画をJSON形式のみで
出力してください（コードフェンス禁止・説明文禁止・JSONオブジェクトのみ）。

# テーマ
{THEME}

# 目標
- 完成尺: 約{TARGET_DURATION}秒（±{TARGET_TOLERANCE}秒）
- 縦型9:16のSNSショート動画（Instagram Reels / TikTok / YouTube Shorts想定）
- テロップは縦書き1文字ずつ画面右端/左端に表示される前提。1カットの情報量は少なめ・短文で。
- 視聴者は動画の冒頭2秒で離脱するかどうかを決めるため、hookは強く

# vertical_hookスタイル特有の構成ルール（絶対厳守）
- shots数の目安: 完成尺÷2秒（例: 15秒なら7個前後）。各ショットのduration_secは1.5〜2.5秒に収める
  高速カット構成にすること（1ショットを長くしない）。
- 5部構成のTTP（テンプレート・トゥ・パターン）に沿って narration_script とshotsを組み立てること:
  1. フック（冒頭1〜2ショット）:「今話題の◯◯」型。テーマを端的に「今話題になっている」トーンで提示する。
  2. 自分ごと化（続く1ショット程度）:「悔しくて調べまくった結果」型。語り手が実際に調べた/試した体験として
     自分ごと化する。
  3. 本題（複数ショット、テンポよく）: 具体的なポイントを2〜4個、テンポよく短く提示する。
  4. 注意喚起・意外性（1ショット程度）: 見落としがちな注意点や意外な事実を提示する。
  5. 締めの一言（最後の1ショット）: 一人称の語りかけ口調で締める。CTAは「保存して見返してね」等の
     汎用的な行動喚起のみとし、特定アカウントへの誘導・プロフィール誘導文言は一切入れないこと。
- narration_scriptは一人称の語りかけ口調（「私は」「〜してみました」「〜だと思います」等）で、
  友人に話しかけるようなカジュアルなトーンにすること。
- caption_jp: 縦書きで映えるよう8〜14字程度の短文を目安にする（40字以内は必須、40字以内でも
  できるだけ短く）。

# 出力してほしい内容
1. concept: この動画の企画コンセプトを1〜2文で。
2. hook: 冒頭2秒で流すフック文（短く強い一言）。
3. narration_script: 日本語のナレーション台本全文（自然な話し言葉、句読点で区切る、一人称の語りかけ口調）。
4. shots: ショットリスト。各ショットは以下を持つこと。
   - id: "s1", "s2", ... の連番
   - visual_prompt: 英語。text-to-video / image-to-video 生成AI向けのビジュアル指示（具体的・簡潔に）
   - motion_preset: 次のいずれか一つ: static, pan_left, pan_right, zoom_in, zoom_out, ken_burns
   - duration_sec: このショットの長さ（秒、1.5〜2.5を目安に。全ショットの合計が目標尺±許容誤差に収まること）
   - caption_jp: 画面に焼き込む日本語キャプション（40字以内、縦書き映えする8〜14字程度の短文目安）
5. bgm_mood: upbeat / calm / emotional / none のいずれか。

# 制約（絶対厳守）
- 景品表示法に違反する断定的な表現（「絶対稼げる」「100%成功」「必ず儲かる」等）は narration_script / caption_jp に一切含めないこと。
- 特定の個人名・競合アカウント名（「みお」「@mio_ai_insta_」等）を一切含めないこと。
- 特定アカウントへの誘導・プロフィール誘導文言（「プロフィールから」「@〜をフォロー」等）は一切含めないこと。
- shots数は「完成尺÷2秒」を目安にし、下限は3個（上限は設けない。完成尺が長い場合はその分shots数を
  増やすこと）。duration_secの合計を目標尺±許容誤差に必ず収めること（これが最優先。shots数や
  各ショットのduration_secはこの合計を満たすように調整してよい）。
- JSON以外の文字列（説明文・コードフェンス```）を一切含めないこと。出力の最初の文字は { 、最後の文字は } であること。

# 出力スキーマ例（このJSON構造のみを出力すること。値は例です）
{SCHEMA_EXAMPLE}
